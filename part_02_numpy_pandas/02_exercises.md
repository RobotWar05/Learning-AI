# Bài tập NumPy và Pandas

1. Tạo NumPy array từ list `[1, 2, 3, 4, 5]` và tính mean.
2. Tạo mảng 2 chiều và in ra shape.
3. Tạo DataFrame gồm cột `name`, `score`, `label`.
4. Lọc các dòng có `score >= 7`.
5. Đếm số lượng từng label.
6. Điền missing value trong cột điểm bằng giá trị trung bình.

## Câu hỏi tự kiểm tra

1. Vì sao NumPy nhanh hơn list Python trong tính toán số?
2. DataFrame khác dict thông thường ở điểm nào?
3. Feature và label trong ML là gì?
